aws-learning-journey/Lemp-S3-RDS/# 
1. Download and install Composer sudo curl -sS https://getcomposer.org/installer | php #
2. Move Composer to a globally accessible location sudo mv composer.phar/usr/local/bin/composer
# 3. Create a symbolic link (optional but recommended) sudo ln -s/usr/local/bin/composer/usr/bin/composer 
# 4. Initialize a composer project (if not already done) composer init # Answer the prompts or skip with --no-interaction
# 5. Install AWS SDK for PHP composer require aws
